<%= form_for @task, method: form_method do | f | %>
     <%= f.label :description %>
     <%= f.text_field :description %>

     <%= f.label :notes %>
     <%= f.text_area :notes %>

     <%= f.submit submit_text %>
<% end %>
