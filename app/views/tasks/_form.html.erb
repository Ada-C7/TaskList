<%= form_for @task, url: tasks_url do |f| %>

<%= f.label :title %>
<%= f.text_field :title %>

<%= f.label :description %>
<%= f.text_field :description, size: 150 %>
<br />
<br />
<%= f.submit "Submit", class: 'new_button' %>
<% end %>
