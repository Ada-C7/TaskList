<h1>Ann's Big 'Ol Task List</h1>
<ul>
  <% @tasks.each do |task| %>
  <li><%= link_to task.name, task_path(task.id) %> : <%= task.status %>
  </li>
  <% end %>
</ul>
<p>Find me in app/views/tasks/index.html.erb</p>
