<h1>Tasks for today:</h1>

<ul>
<% @tasks.each do |task| %>

<li>
  <%= link_to task.name, task_path(task.id)%>
   - Description: <%=task.description%>
   - When completed: <%=task.completed_at%>
   <%=button_to "Delete this task", task_path(task.id), method: :delete, confirm: "Are you sure?", class:"delete_button"  %>


<!-- <%= link_to 'Delete this task',url_for(action: :delete,id: task.id), method: :delete, confirm: "Are you sure?" %> -->
<!-- <%= link_to 'Delete this task', task_path(task.id),  :method => :delete, confirm: "Are you sure?" %> -->

<!--
   <a href="/tasks/:id" data-method="delete" data-confirm="Are you sure?">Delete this task</a> -->


</li>

<% end%>

</ul>

<%= link_to "Create a new task", new_task_path %>
