<h1>All Tasks</h1>
<ul>
     <% @tasks.each do |task| %>
          <li> <%= link_to task.description, task_path(task.id) %>  <%= link_to "Complete", complete_task_path(task.id), method: :put %>
 </li>
     <% end %>
</ul>

<p> <%= link_to "Add New Task", new_task_path %> <p>
