<h1>Tasks</h1>
<h4>Hello and welcome to your task list</h4>

<ul>
  <% @tasks.each do |task| %>
  <li><%= link_to task.name, task_path(task.id) %> </li>
  <% end %>
</ul>

<p><%= link_to "add a new task", tasks_new_path %></p>
