<main>
  <section id="list">
    <h1>Task List</h1>

    <ul>
      <% @tasks.each do |task| %>
      <li>
        <%= link_to task.what, task_path(task.id) %>
      </li>
      <% end %>
    </ul>
  </section>
</main>
