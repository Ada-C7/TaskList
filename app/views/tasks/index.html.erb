<h1>Task List</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= task[:what] %></li>
    <% end %>
  </ul>

  <ul>
    <% @tasks.each_with_index do |task, id| %>
      <li>
        <%= link_to task[:what], task_path(id) %>
      </li>
      <% end %>
    </ul>
