<h1>Tasks</h1>

<section class="index">
  <ul>
    <% @tasks.each do |task| %>
    <li>
      <%= link_to task.name, task_path(task.id) %>
    </li>
    <% end %>
  </ul>
  <p>
    <%= link_to "Add Task", "/tasks/new" %>
  </p>
</section>
