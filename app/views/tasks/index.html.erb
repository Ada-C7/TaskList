  <section id="list">
    <h1>Task List</h1>

    <ul>
      <% @tasks.each do |task| %>
      <li>
        <%= link_to task.what, task_path(task.id) %>
      </li>
      <% end %>
    </ul>

    <p id="new_task"><%= link_to "Add a new task", new_task_path %></p>

  </section>
