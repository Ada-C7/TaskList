<h1>Jou-Jou's To-do List</h1>

<img src="http://placebeyonce.com/460-460" />

<h4>
  <ul>
    <% @tasks.each do |task| %>
      <li>
        <%= link_to task.action, task_path(task.id) %>
      </li>
    <% end %>
  </ul>
</h4>
