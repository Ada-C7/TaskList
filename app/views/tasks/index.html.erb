<h1>Tasks List</h1>
<%= link_to "Create a new task", new_task_path %>

<div>
  <ul>
    <% @tasks.each do |task| %>
    <li>
      <%= link_to task[:name], task_path(task.id) %>
    </li>

    <% end %>
  </ul>
</div>
