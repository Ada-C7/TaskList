<h1>Tasks</h1>

<ul>
  <% @tasks.each_with_index do |task, index| %>
  <li>
    <%= link_to task[:name], tasks_path(index + 1) %>
  </li>

  <% end %>
</ul>

<%= link_to "Add a new task", new_task_path %>
