<li>

  <%=link_to "#{task.name.capitalize}",task_path(task.id)%>

  <%= button_to "Delete", task_path(task),method: :delete%>
  <%= button_to "Mark Complete", task_path(task),method: :patch, params:{completed: true}%>


</li>
